# Virtual environment
.venv/
venv/
env/

# Secrets — NEVER commit these
.env

# Python cache files
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# OS noise
.DS_Store
Thumbs.db

# Local near_miss_audit.py runs — persisted via the near_miss_audit.yml
# workflow's GitHub Actions artifact instead, not committed to the repo.
audit_output/

# Personal documents — this repo is public; never commit resumes or similar
# files dropped into the project folder for reference.
*.docx
