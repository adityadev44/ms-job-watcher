# Virtual environment
.venv/
venv/
env/

# Secrets — NEVER commit these
.env

# Python cache files
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# OS noise
.DS_Store
Thumbs.db

# Personal documents — this repo is public; never commit resumes or similar
# files dropped into the project folder for reference.
*.docx
