# Job Watcher Configuration
# Safe operational settings live here; notification credentials are read only
# from environment variables / GitHub Secrets by src/notifier.py.

# YAML anchors keep the intentionally shared search policy in one place while
# allowing individual companies to override ATS-specific limits and exceptions.
_defaults:
  keywords: &default_keywords
    - "software engineer"
    - "senior software engineer"
    - ".NET developer"
    - "C# developer"
    - "dot net"
    - "angular"
    - "AI engineer"
    - "machine learning engineer"
    - "python developer"
    - "generative ai engineer"
  india_locations: &india_locations
    - "India"
  exclude_locations: &default_exclude_locations
    - "Chennai"
    - "Tamil Nadu"
    - "Pune"
    - "Chandigarh"
    - "Kochi"
    - "Kerala"
    - "Trivandrum"
    - "Lucknow"
    - "Nagpur"
    - "Madurai"
    - "Kolkata"
    - "Indore"

search:
  max_listings: 350
  inter_page_delay: 0.1
  keywords:
    - "software engineer"
    - "senior software engineer"
    - "software development engineer"
    - "SDE"
    - "full stack engineer"
    - "backend engineer"
    - ".NET developer"
    - "C# developer"
    - "AI engineer"
    - "machine learning engineer"
    - "python developer"
    - "generative ai engineer"
  locations: []
  exclude_locations: *default_exclude_locations

optum_search:
  max_listings: 200
  inter_page_delay: 0.1
  keywords:
    - "software engineer"
    - "senior software engineer"
    - "software development engineer"
    - "SDE"
    - ".NET developer"
    - "C# developer"
    - "dot net"
    - "angular"
    - "AI engineer"
    - "machine learning engineer"
    - "python developer"
    - "generative ai engineer"
  locations: *india_locations
  exclude_locations: *default_exclude_locations

amazon_search:
  max_listings: 200
  inter_page_delay: 0.1
  keywords:
    - "software engineer"
    - "senior software engineer"
    - "software development engineer"
    - "SDE"
    - ".NET developer"
    - "C# developer"
    - "dot net"
    - "angular"
    - "AI engineer"
    - "machine learning engineer"
    - "python developer"
    - "generative ai engineer"
  locations: []
  exclude_locations: *default_exclude_locations

matching:
  # Include a job if its title belongs to the software-engineer family.
  # Matching is normalised: case, hyphens, C#/CSharp/.NET/dotnet, and
  # Roman numerals (II/III) are treated as equal to Arabic (2/3).
  # No specific seniority is required — any level in the family passes.
  title_family:
    - "software engineer"
    - "software engr"
    - "software senior engineer"
    - "software developer"
    - "software development engineer"
    - "SDE"
    - "SWE"
    - "full stack engineer"
    - "backend engineer"
    - "full-stack developer"
    - "backend developer"
    - ".NET developer"
    - "application developer"
    - "application engineer"
    - "staff engineer"
    - "senior staff engineer"
    - "associate staff engineer"
    - "senior engineer"
    - "machine learning engineer"
    - "ml engineer"
    - "ai engineer"
    - "artificial intelligence engineer"
    - "generative ai engineer"
    - "genai engineer"
    - "llm engineer"
    - "applied ai engineer"
    - "ai platform engineer"
    - "python developer"
    - "python engineer"

  # Keep a job only if its description mentions at least one of these skills.
  # A job must match BOTH this list AND primary_skills (below) — they are
  # independent checks — so every primary_skills term (every group) MUST also
  # be listed here, plus the soft/broad-only signals that never qualify alone.
  skills:
    - ".NET"
    - ".NET Core"
    - ".NET Framework"
    - "dotnet"
    - "C#"
    - "ASP.NET"
    - "Web API"
    - "SQL Server"
    - "T-SQL"
    - "Azure"
    - "Angular"
    - "TypeScript"
    - "React"
    - "Entity Framework"
    - "Python"
    - "machine learning"
    - "artificial intelligence"
    - "Golang"
    - "multi-agent"
    - "agentic"
    - "prompt engineering"
    - "OpenAI"
    - "Anthropic"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

  # At least one "hard" skill from ONE of these named groups must appear in the
  # description — a job qualifies via EITHER track, not both. Broad-only terms
  # from `skills` above (Azure/Angular/TypeScript/React/Python/machine learning/
  # etc.) are NOT sufficient alone; they appear in too many unrelated postings.
  # Leave a group empty (or the whole mapping empty) to disable that check.
  #
  # This mapping is also the source of the "[tag]" prefix matcher.py attaches
  # to each alert (see src/matcher.py find_matching_jobs) so Shivangi can tell
  # .NET/C# hits apart from AI/ML/Python hits in one combined Telegram/Gmail
  # feed. Group names are shown verbatim in the alert — keep them short.
  #
  # Deliberately excludes bare "AI", "ML", "RAG", "LLM", "GPT", "Go" everywhere
  # in this file: matcher.py's substring check is NOT word-boundary aware, and
  # those short tokens are substrings of extremely common words in ordinary
  # job descriptions — "RAG" alone matches inside "storage"/"average"/"garage",
  # "Go" matches inside "going"/"ongoing"/"algorithm", "ML" matches inside
  # "HTML". Use full distinctive terms (LangChain, generative ai, large
  # language model, Golang, ...) instead — same signal, no false positives.
  #
  # "SQL Server"/"T-SQL"/"Web API" are deliberately NOT in the .NET/C# group
  # below (kept only in the broader `skills` list) — they're a database and a
  # generic REST-API phrase any language's app can use, not .NET-specific.
  # Confirmed 2026-08-31: a Citi "Senior Python Application Developer" JD got
  # wrongly tagged [.NET / C#] purely because it mentioned "SQL Server" as its
  # database — the actual FI Risk role was fine ("Java (or .NET Core)"
  # explicitly accepts either), but the Python role's mistagging was a real
  # false positive Shivangi flagged directly. This was already a known
  # weakness before the AI/ML track existed (see the per-company
  # require_tech_in_description comments below — several explicitly call out
  # "SQL Server/EF" as a shared-primary-skill false-positive risk) but only
  # started producing visibly wrong TAGS once a second track existed for a
  # job to be wrongly attributed to.
  primary_skills:
    ".NET / C#":
      - ".NET"
      - ".NET Core"
      - ".NET Framework"
      - "dotnet"
      - "C#"
      - "ASP.NET"
      - "Entity Framework"
    "AI / ML / Python":
      - "LangChain"
      - "LangGraph"
      - "RAGAS"
      - "Langfuse"
      - "LangSmith"
      - "CrewAI"
      - "vector database"
      - "ChromaDB"
      - "Pinecone"
      - "generative ai"
      - "large language model"
      - "retrieval augmented generation"

  # Reject a job immediately if its title contains any of these terms, as a
  # whole word/phrase (matched via word boundaries, not plain substring — so
  # "VP" rejects "VP - Engineering" but not "AVP"/"SVP", and "director"
  # rejects "Director of Engineering" but not "Active Directory").
  exclude_terms:
    - "intern"
    - "internship"
    - "trainee"
    - "apprentice"
    - "fresher"
    - "graduate"
    - "new grad"
    - "university"
    - "principal"
    - "director"
    - "vice president"
    - "VP"
    - "head of"
    - "engineering manager"
    - "manager"
    - "mechanical"
    - "electrical"
    - "industrial"
    - "hardware"
    - "firmware"
    - "embedded"
    - "datacenter technician"
    - "network engineer"
    - "sales engineer"
    - "solutions engineer"
    - "customer engineer"
    - "support engineer"
    - "data scientist"

siemens_search:
  max_listings: 200
  inter_page_delay: 0.1
  keywords:
    - "software engineer"
    - "senior software engineer"
    - "software development engineer"
    - ".NET developer"
    - "C# developer"
    - "dot net"
    - "angular"
    - "AI engineer"
    - "machine learning engineer"
    - "python developer"
    - "generative ai engineer"
  locations: *india_locations
  exclude_locations: *default_exclude_locations

honeywell_search:
  max_listings: 200
  inter_page_delay: 0.1
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

wellsfargo_search:
  max_listings: 200
  inter_page_delay: 0.2
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  # Wells Fargo-specific: description must explicitly name a .NET/C# term OR
  # an AI/LLM production term. Filters out generic "Senior Software Engineer"
  # titles that are plain Java roles even when a shared primary_skill
  # (SQL Server/EF) shows up in passing — a Java role mentioning LangChain in
  # passing would still need the explicit list above to actually pass.
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

dell_search:
  max_listings: 200
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

oracle_search:
  max_listings: 200
  inter_page_delay: 0.3
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

spglobal_search:
  max_listings: 300
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

chubb_search:
  max_listings: 200
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

fis_search:
  max_listings: 500
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

metlife_search:
  max_listings: 200
  inter_page_delay: 0.5
  # MetLife's search engine does not filter reliably by tech keyword.
  # A single empty keyword fetches all ~95 India jobs; title/skills filters handle the rest.
  keywords:
    - ""
  locations: *india_locations
  exclude_locations: *default_exclude_locations

gallagher_search:
  max_listings: 200
  inter_page_delay: 0.2
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

spglobal_careers_search:
  max_listings: 300
  inter_page_delay: 0.2
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

morningstar_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Morningstar's Phenom People site has no server-side keyword filtering.
  # One keyword suffices — all India jobs are fetched and cached in one scan.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

wtw_search:
  max_listings: 200
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

icertis_search:
  max_listings: 200
  inter_page_delay: 0.3
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

maersk_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Maersk uses Workday with India location WIDs embedded in the fetcher.
  # Keyword filtering is not server-side — all India jobs are cached once per run.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

nomura_search:
  max_listings: 400
  inter_page_delay: 0.3
  # Nomura India portal fetches all India jobs at once — keywords are not sent server-side.
  # All 337+ India jobs are cached in-module; title/skills filters handle relevance.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

fiserv_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Fiserv's Workday tenant exposes no country-level location facet — fetched
  # globally per keyword and filtered for India client-side (locationsText).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

fidelity_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Fidelity's Workday tenant has a clean locationCountry facet for India.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

morganstanley_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Eightfold-hosted (morganstanley.eightfold.ai) — same API shape as Microsoft.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

amex_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Oracle HCM CE tenant egug.fa.us2.oraclecloud.com, site CX_1.
  # India location facet ID 300000000228786 confirmed via live search.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

marshmclennan_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant "mmc", site "MMC" — covers Marsh, Mercer, Guy Carpenter, Oliver Wyman.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

jpmorgan_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Oracle HCM CE, raw tenant jpmc.fa.oraclecloud.com, site CX_1001.
  # India location facet ID 300000000289360.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

synchrony_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant "synchronyfinancial", site "careers". No country-level
  # facet — uses India city/region location WIDs (Hyderabad + Remote Region
  # IN) instead. Tenant hard-caps page size at 20 (HTTP 400 above that).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

mastercard_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant "mastercard", site "CorporateCareers". No country facet —
  # 8 India city WIDs (Gurgaon, Hyderabad, Mumbai, Navi Mumbai, Pune, Vadodara)
  # passed under the "locations" facet key.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

goldmansachs_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Public site (higher.gs.com) is a Next.js/Okta-gated frontend; the real
  # backend is Oracle HCM CE at hdpc.fa.us2.oraclecloud.com, site
  # LateralHiring, open to unauthenticated requests. Same pattern as Chubb/
  # Amex/JPMorgan Chase.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

nagarro_search:
  max_listings: 500
  inter_page_delay: 0.2
  # SmartRecruiters (careers.smartrecruiters.com/nagarro1). The "q" keyword
  # param is a loose server-side filter only — title/skill filters do the
  # real work. Nagarro's titles are level-based ("Staff Engineer, .Net
  # Fullstack") rather than role-based; "staff engineer"/"senior engineer"
  # were added to the shared matching.title_family to surface these.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

citi_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant citi.wd5.myworkdayjobs.com, site "2". Uses
  # Country_and_Jurisdiction facet (not locationCountry). India WID
  # c4f78be1a8f14da0ab49ce1162348a5e. ~339 India SW engineer jobs.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

bny_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Oracle HCM CE (not Workday). Tenant: eofe.fa.us2.oraclecloud.com,
  # site BNY-Careers. India location facet ID 300000000378365.
  # Majority of India jobs are in Pune (excluded) — 0 matches expected
  # until BNY opens non-Pune .NET roles.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

northerntrust_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant ntrs.wd1.myworkdayjobs.com, site northerntrust.
  # locationCountry WID c4f78be1a8f14da0ab49ce1162348a5e (same cross-tenant
  # India GUID as Fidelity/Wells Fargo). Page size capped at 20.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

deutsche_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Deutsche Bank uses Beesite (api-deutschebank.beesite.de/search/) as
  # frontend wrapping Workday (db.wd3.myworkdayjobs.com) for descriptions.
  # Keywords and country filter are ignored server-side — all ~1808 global
  # jobs are fetched and India filtered client-side (CountryCode==IN).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

barclays_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant barclays.wd3.myworkdayjobs.com. No locationCountry facet —
  # uses 11 India city WIDs in appliedFacets.locations (same pattern as
  # Mastercard). locationsText omits "India" — appended client-side.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

ubs_search:
  max_listings: 200
  inter_page_delay: 0.3
  # IBM BrassRing (Kenexa) at jobs.ubs.com. Requires CSRF token (RFT header)
  # extracted per session. ~15 India jobs total — mostly non-tech roles.
  # Descriptions embedded in search results; no separate detail fetch needed.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

accenture_search:
  max_listings: 500
  inter_page_delay: 0.3
  # Workday wd103 tenant (accenture.wd103.myworkdayjobs.com/AccentureCareers).
  # India WID c4f78be1a8f14da0ab49ce1162348a5e (locationCountry facet).
  # ~24,731 India jobs; API caps at 2000 results per query.
  # require_tech_in_description is MANDATORY — without it thousands of
  # generic "Custom Software Engineer" titles flood through.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

infosys_search:
  max_listings: 500
  inter_page_delay: 0.3
  # Custom in-house ATS at intapgateway.infosysapps.com/careersci/.
  # searchText param is a no-op — all ~1558 India jobs returned per call.
  # Descriptions bundled in list response (no separate detail fetches).
  # require_tech_in_description is MANDATORY — generic titles dominate.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

cognizant_search:
  max_listings: 500
  inter_page_delay: 0.3
  # Custom Umbraco CMS site (careers.cognizant.com). RSS feed at
  # /global-en/jobs/xml/?rss=true returns all ~2069 jobs with full
  # descriptions — no per-job fetch needed. India filtered client-side
  # via <country> field. require_tech_in_description is MANDATORY.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

tcs_search:
  max_listings: 500
  inter_page_delay: 0.3
  # TCS iBegin portal (ibegin.tcsapps.com). India-only portal — no location
  # filter needed. Keyword search is extremely loose (# breaks search);
  # all 4227 India jobs match "C#". require_tech_in_description is MANDATORY.
  # 10 jobs per page (fixed); walk-in jobs (IDs ending in W) are skipped.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

capgemini_search:
  max_listings: 500
  inter_page_delay: 0.3
  # SAP SuccessFactors J2W at careers.capgemini.com. HTML scraping (same
  # platform as Nomura but with query-string pagination: startrow=N, 25/page).
  # locationsearch=india applied server-side. Thousands of India jobs —
  # mostly SAP/Java. require_tech_in_description is MANDATORY.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

lseg_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant lseg.wd3.myworkdayjobs.com, site "careers". locationCountry
  # facet with the standard India WID. Large Bengaluru engineering centre with
  # frequent .NET/C#/Azure roles. locationsText uses office codes
  # ("IND-BLR-...") — ", India" appended in the fetcher.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

statestreet_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant statestreet.wd1.myworkdayjobs.com, site "Global". Uses the
  # capitalised Location_Country facet key (like Marsh McLennan). ~200 India
  # jobs across Bangalore, Hyderabad, Mumbai, Coimbatore. Coimbatore is in
  # Tamil Nadu but the location text doesn't say so — excluded by name.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations:
    - "Chennai"
    - "Tamil Nadu"
    - "Coimbatore"
    - "Pune"
    - "Chandigarh"
    - "Kochi"
    - "Kerala"
    - "Trivandrum"
    - "Lucknow"
    - "Nagpur"
    - "Madurai"
    - "Kolkata"
    - "Indore"
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

broadridge_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant broadridge.wd5.myworkdayjobs.com, site "Careers".
  # Location_Country facet (capitalised key). Broadridge is heavily .NET —
  # small India job count (~30) but high relevance when roles open.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

kyndryl_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant kyndryl.wd5.myworkdayjobs.com, site
  # "KyndrylProfessionalCareers". locationCountry facet. ~290 India software
  # jobs; multi-site rollups show "2 Locations" — ", India" appended.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

dxc_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant dxctechnology.wd1.myworkdayjobs.com, site "DXCJobs".
  # locationCountry facet. locationsText format is "IND - TN - CHENNAI"
  # (state codes, no "India") — the fetcher appends ", India"; "- TN -"
  # below excludes ALL Tamil Nadu cities, which "Tamil Nadu" alone can't.
  # IT-services shop with generic titles — require_tech_in_description
  # narrows to jobs whose JD explicitly names .NET/C#/ASP.NET or AI/ML, not just
  # any shared primary_skill (SQL Server/EF alone could be a Java role).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations:
    - "Chennai"
    - "Tamil Nadu"
    - "- TN -"
    - "Pune"
    - "Chandigarh"
    - "Kochi"
    - "Kerala"
    - "Trivandrum"
    - "Lucknow"
    - "Nagpur"
    - "Madurai"
    - "Kolkata"
    - "Indore"
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

ameriprise_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant ameriprise.wd5.myworkdayjobs.com, site "ameriprise".
  # locationCountry facet. India centres: Hyderabad, Noida, Gurugram.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

factset_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant factset.wd108.myworkdayjobs.com, site "FactSetCareers".
  # No country facet — fetches globally (~60 jobs total) and filters India
  # client-side. FactSet's Hyderabad centre is .NET-heavy.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

paypal_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant paypal.wd1.myworkdayjobs.com, site "jobs". No country
  # facet — fetches globally, filters India client-side with a word-boundary
  # match (so "Indianapolis"/"Indiana" never pass). India presence is
  # currently small (Bangalore + Chennai) — 0 matches often expected.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

invesco_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant invesco.wd1.myworkdayjobs.com, site "IVZ". No country
  # facet and locationsText omits "India" entirely ("Hyderabad, Telangana") —
  # the fetcher detects India via city/state tokens and appends ", India".
  # Invesco's Hyderabad centre is .NET-heavy.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

firstamerican_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant firstam.wd1.myworkdayjobs.com, site "faicareers" — First
  # American (India)'s dedicated portal; every posting is an India job (all
  # Bangalore currently), no country facet needed. First American is a
  # heavily .NET shop. locationsText ("IND, Karnataka, Bangalore") gets
  # ", India" appended in the fetcher.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

standardchartered_search:
  max_listings: 400
  inter_page_delay: 0.2
  # SAP SuccessFactors "Job2Web Unify" tenant (jobs.standardchartered.com).
  # keywords are ignored server-side -- facetFilters restricts to India
  # (~280 jobs) and all are cached in-module; title/skill filters do the
  # real narrowing.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

wipro_search:
  max_listings: 4000
  inter_page_delay: 0.2
  # Same SuccessFactors Unify platform as Standard Chartered, different
  # tenant (careers.wipro.com). ~3700 India jobs cached in-module; almost
  # entirely generic IT-services titles ("SOFTWARE ENGINEER L3" etc with no
  # tech named) -- require_tech_in_description is MANDATORY (was a dead
  # require_tech_in_title config before this change — never actually
  # applied by run_wipro.py, same bug HCLTech had).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

hcltech_search:
  max_listings: 8500
  inter_page_delay: 0.2
  # Same SuccessFactors Unify platform, tenant careers.hcltech.com, India-only
  # category page (categoryId 9553955). ~8000 India jobs cached in-module;
  # overwhelmingly generic IT-services titles -- require_tech_in_description
  # is MANDATORY (narrower than the shared primary_skills check: only
  # .NET/C#/ASP.NET or AI/ML core terms count, not SQL Server/EF/Web API alone,
  # since boilerplate JDs namedrop many languages' skills). NOTE: this used
  # to be require_tech_in_title, but the runner never actually applied it
  # (dead config) -- replaced with a working description-based check.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

hsbc_search:
  max_listings: 10
  inter_page_delay: 0.3
  # Eightfold (portal.careers.hsbc.com); HSBC's tenant has the normal public
  # search API (pcsx/search) disabled -- only the "related jobs" widget
  # endpoint works, and it hard-caps at 10 results with no pagination. Set
  # max_listings to 10 (not the usual 200+) so find_matching_jobs stops
  # after one page instead of retrying for pages that will never come.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

msci_search:
  max_listings: 50
  inter_page_delay: 0.2
  # Algolia-powered (careers.msci.com) -- small total job count (~90
  # globally, ~18 India) so a single unfiltered query covers everything;
  # cached in-module.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

target_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant target.wd5.myworkdayjobs.com, site "targetcareers".
  # Target's India GCC is in Bangalore. Capitalised "Location_Country" facet
  # key. ~58 India software-engineer jobs.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

adobe_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant adobe.wd5.myworkdayjobs.com, site "external_experienced".
  # India centres in Bangalore + Noida. locationCountry facet works even
  # though not listed in the tenant's advertised facets response.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

micron_search:
  max_listings: 500
  inter_page_delay: 0.2
  # Workday tenant micron.wd1.myworkdayjobs.com, site "External". Hyderabad
  # "Phoenix Aquila" campus. Semiconductor/hardware company -- most roles
  # are firmware/data, not .NET, so low match volume expected.
  # WARNING: locationCountry facet is unreliable on this tenant (~85% of
  # "India"-faceted results are actually Singapore/Taiwan/Boise) -- the
  # fetcher does NOT append ", India" and relies on matcher.py's
  # is_india_job() to filter genuinely.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

sabre_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant sabre.wd1.myworkdayjobs.com, site "SabreJobs". Travel-
  # technology (GDS) company with a Bengaluru engineering centre.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

autodesk_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant autodesk.wd1.myworkdayjobs.com, site "Ext". India centres
  # in Bengaluru + Pune. locationsText uses abbreviated "IND" country code.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

verizon_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant verizon.wd12.myworkdayjobs.com, site "verizon-careers".
  # India engineering presence in Hyderabad (network/telecom software).
  # WARNING: locationCountry facet is unreliable on this tenant (genuine
  # US locations like Arlington TX/Ashburn VA leak through) -- the fetcher
  # does NOT append ", India" and relies on matcher.py's is_india_job()
  # to filter genuinely.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

lowes_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant lowes.wd5.myworkdayjobs.com, site "LWS_External_CS".
  # Lowe's India engineering centre is in Bengaluru.
  # WARNING: locationCountry facet is unreliable on this tenant (genuine US
  # locations like Perris CA/Richmond VA/Charlotte NC HQ leak through) --
  # the fetcher appends ", India" only for recognised India city names
  # (Bengaluru etc), not blindly.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

ebay_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant ebay.wd5.myworkdayjobs.com, site "apply". eBay's India
  # engineering centre is in Bengaluru. Capitalised "Location_Country" facet.
  # require_tech_in_description narrows to jobs whose JD explicitly names
  # .NET/C#/ASP.NET or AI/ML, not just any shared primary_skill.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

generalmotors_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant generalmotors.wd5.myworkdayjobs.com, site "Careers_GM".
  # GM India Technical Centre in Bengaluru -- small footprint (~5 total
  # "software engineer" postings currently); included for completeness,
  # same precedent as BNY Mellon/Icertis. Capitalised "Location_Country"
  # facet key.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

bankofamerica_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Custom AEM-backed servlet at careers.bankofamerica.com/services/
  # jobssearchservlet. Keyword/country params are ignored server-side —
  # ~1760 global jobs fetched and cached in one pass, ~62 India (mostly
  # Chennai/Mumbai/Hyderabad; Chennai excluded by default). require_tech_in_
  # description narrows GBS technology postings to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

ltimindtree_search:
  max_listings: 200
  inter_page_delay: 0.2
  # careers.ltimindtree.com's public search has ~80 overseas-only roles with
  # zero India results — actual India hiring runs through a separate
  # RippleHire-hosted site (ltimindtree.ripplehire.com), linked from
  # ltm.com/india-careers. Unlike most fetchers here, RippleHire applies the
  # keyword server-side, so normal per-keyword pagination works.
  # require_tech_in_description narrows generic "Specialist - Software
  # Engineering" titles (no tech signal) to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

persistent_search:
  max_listings: 800
  inter_page_delay: 0.2
  # Zwayam ATS (apipersistent.zwayam.com). Search keyword is unreliable
  # (loose OR-match) so all ~700 global postings are cached once and
  # filtered client-side for India (~10-15% of the pool). City is parsed
  # from the jobUrl slug since the API's own location field is just
  # "India" — without it Pune (Persistent's HQ, heavy postings there)
  # wouldn't be excluded. require_tech_in_description narrows a large
  # multi-stack IT-services shop to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

genpact_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday tenant genpact.wd108.myworkdayjobs.com, site External_Careers.
  # No usable location facet exposed (locationMainGroup has no India
  # sub-value) — global fetch per keyword (searchText narrows the ~2000-
  # capped total) + client-side India filter via locationsText.
  # require_tech_in_description narrows a large multi-stack BPO/IT-services
  # shop to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

ibm_search:
  max_listings: 400
  inter_page_delay: 0.2
  # Elasticsearch-backed search API at www-api.ibm.com/search/api/v2.
  # field_keyword_05 == "India" facet filters server-side (~370 jobs for
  # "software engineer"). Job-detail pages sit behind AWS WAF bot-challenge
  # tokens — plain requests get a 202/empty body, so description fetch uses
  # headless Firefox (Playwright) to solve the challenge, same pattern as
  # Honeywell. require_tech_in_description narrows enterprise-consulting
  # boilerplate JDs to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

techmahindra_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Legacy ASP.NET WebForms site (careers.techmahindra.com) with an AJAX
  # UpdatePanel search form — country-select fires a partial postback that
  # needs exact ViewState/EventValidation/X-MicrosoftAjax headers to
  # replicate; fetcher drives headless Firefox instead (same approach as
  # Honeywell). ~81 India jobs, keyword not applied server-side — shared
  # title/skill filters do the real narrowing. Job-detail pages ARE plain
  # server-rendered HTML (no browser needed there).
  # require_tech_in_description narrows a large multi-stack IT-services
  # shop to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

virtusa_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Taleo ATS (virtusa.taleo.net). Search REST endpoint
  # (careersection/rest/jobboard/searchjobs) needs TZ/tzname/X-Requested-With
  # headers matching the browser or it 500s; India filtered server-side via
  # LOCATION facet id 200100250 (~720 jobs). Job-detail description fetch
  # uses headless Firefox (Playwright) since the real detail endpoint is a
  # ~13KB JSF-postback form too fragile to hand-replicate.
  # require_tech_in_description narrows a large multi-stack IT-services shop
  # to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

hexaware_search:
  max_listings: 300
  inter_page_delay: 0.3
  # Oracle HCM CE (not Workday). Tenant fa-etqo-saasfaprod1.fa.ocs.oraclecloud.com,
  # site CX_1. Same REST pattern as Chubb/Amex/Icertis — plain requests, no
  # Playwright. No location facet ID used — fetches globally per keyword and
  # filters India client-side via PrimaryLocation, same conservative approach
  # as Icertis/WTW. require_tech_in_description narrows a large multi-stack
  # IT-services shop to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

societegenerale_search:
  max_listings: 300
  inter_page_delay: 0.2
  # careers.societegenerale.com's own client-side search API needs a bearer
  # token that 403s for anything but the page's own first-load request
  # (even replayed from within the same browser session) — not viable.
  # The "all-job-offers" listing page is fully server-rendered instead: one
  # plain GET returns all ~694 postings in a single page, no pagination.
  # Job-detail pages carry a clean schema.org JobPosting JSON-LD block
  # (plain-text description + datePosted). require_tech_in_description
  # narrows a large multi-department bank to explicit .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

schwab_search:
  max_listings: 100
  inter_page_delay: 0.2
  # www.schwabjobs.com — iCIMS-backed (career-ind-schwab.icims.com handles
  # Apply), but search/listing pages are plain server-rendered HTML, no API
  # needed. India filtered server-side via /search-jobs/india path (~19
  # jobs, small Hyderabad presence). Job-detail pages carry a clean
  # schema.org JobPosting JSON-LD block. require_tech_in_description
  # narrows to jobs whose JD explicitly names .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

groww_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Greenhouse board token "groww" (job-boards.eu.greenhouse.io/groww). Keyword
  # ignored server-side, full board cached in-module. DO NOT add
  # require_tech_in_description — not enabled per playbook default.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

razorpay_search:
  max_listings: 50
  inter_page_delay: 0.2
  # Greenhouse board token razorpaysoftwareprivatelimited; ~21 total jobs,
  # keyword/location ignored server-side, full board cached in-module.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

cred_search:
  max_listings: 20
  inter_page_delay: 0.1
  # Lever board token "cred" (~14 postings). Keyword/location ignored
  # server-side, full board cached once. DO NOT add
  # require_tech_in_description — board is small, titles descriptive enough.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

meesho_search:
  max_listings: 200
  inter_page_delay: 0.1
  # Lever board token "meesho" (~48 postings). Keyword/location ignored
  # server-side, full board cached once.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

paytm_search:
  max_listings: 250
  inter_page_delay: 0.1
  # Lever board token "paytm" (~228 postings, ~218 India). Keyword/location
  # ignored server-side, full board cached once. DO NOT add
  # require_tech_in_description — titles varied but Layer 3 should suffice.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

eclerx_search:
  max_listings: 300
  inter_page_delay: 0.3
  # Oracle HCM CE (not Workday). Tenant fa-ewji-saasfaprod1.fa.ocs.oraclecloud.com,
  # site CX_1. Same REST pattern as Chubb/Hexaware/Icertis/WTW — plain requests,
  # no Playwright. No location facet ID used — fetches globally per keyword and
  # filters India client-side via PrimaryLocation. require_tech_in_description
  # active: eClerx is a KPO/BPO shop whose India postings are almost entirely
  # generic ops titles (Senior Analyst, Process Manager) with zero "engineer"/
  # "developer" titles seen live — narrows any future SWE-titled req to explicit
  # .NET/C#/ASP.NET or AI/ML.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

macquarie_search:
  max_listings: 200
  inter_page_delay: 0.15
  # Avature ATS at recruitment.macquarie.com (NOT the mq.wd3.myworkdayjobs.com
  # Workday tenant — that's Macquarie University, a different organization
  # that happens to share the name). Plain HTML scraping, no Playwright.
  # Keyword search is genuinely server-side; location is not — India means
  # exactly Gurugram/Hyderabad, filtered client-side via a city whitelist
  # since location text never says "India".
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

mufg_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant mufgub.wd3.myworkdayjobs.com, site "MUFG-Careers". Uses a
  # plain "Country" facet (not locationCountry/Country_and_Jurisdiction).
  # India WID c4f78be1a8f14da0ab49ce1162348a5e; ~214 India jobs. Tenant
  # HTTP 400s above limit=20 — fetcher clamps num defensively.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

ing_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant ing.wd3.myworkdayjobs.com, site ICSGBLCOR (careers.ing.com
  # is a Radancy/TalentBrew skin over this). No location facet at all --
  # fetches globally per keyword (searchText IS honored server-side) and
  # filters India client-side via word-boundary regex. Confirmed live
  # 2026-08-30: 0 of 734 global jobs are in India -- 0 matches expected
  # until ING opens India roles; this is a genuine current fact, not a bug.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

saplabs_search:
  max_listings: 200
  inter_page_delay: 0.2
  # SAP SuccessFactors J2W classic (jobs.sap.com, company=SAP). Server-rendered
  # HTML, no CSRF/session dance needed (not the Unify theme). Pagination via
  # ?startrow=N (25/page). Keyword search full-text-matches description
  # content so common tech words converge on nearly the whole ~50-job India
  # pool (functionally similar to _IGNORES_KEYWORDS companies even though not
  # classified there). require_tech_in_description active: SAP is a large
  # multi-stack employer (Java/Go/Python/HANA-heavy), so Layer 3's broader
  # .NET/C# group risks false positives on SQL Server/EF alone.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

lenskart_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Custom "ainterviews.com" ATS (not one of the standard vendors). Whole
  # board (~67 postings) fetched and cached once — keyword/location ignored
  # server-side, description already inline. Location text is bare city
  # names ("Bangalore", "Gurugram") with no "India" word — ", India" appended
  # only for recognized India cities (Razorpay/CRED/Invesco convention);
  # genuine non-India offices (Spain, Singapore, UAE, Japan, Thailand, Italy)
  # left untouched.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

mphasis_search:
  max_listings: 200
  inter_page_delay: 0.2
  # RippleHire (mphasis.ripplehire.com, token ty4DfyWddnOrtpclQeia) — same ATS
  # as LTIMindtree. Keyword search IS genuinely server-side (counts differ per
  # keyword). No usable location facet — "geo"/"location" params zero out
  # totalJobCount even though India jobs exist; fetches globally per keyword
  # and recognizes India via a city whitelist (bare city names, no country
  # field). require_tech_in_description active: titles are heavily generic/
  # level-banded ("Senior Software Engineer") hiding wildly different stacks
  # (Salesforce LWC, Selenium/UFT, Java/AWS all seen under that exact title) —
  # narrows to explicit .NET/C#/ASP.NET or AI/ML signal in the description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

necsws_search:
  max_listings: 150
  inter_page_delay: 0.2
  # SmartRecruiters (careers.smartrecruiters.com/NECSWS) — same ATS as Nagarro
  # and PhonePe. "country=in" is a reliable server-side filter (verified zero
  # leakage). Keyword param does loose pre-filtering only, not a hard filter —
  # shared title/skill checks do the real work, same as Nagarro. India titles
  # name the stack directly ("Senior Software Engineer - .Net Core and
  # Cloud") rather than generic banding — require_tech_in_description left
  # off per playbook default.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

phonepe_search:
  max_listings: 100
  inter_page_delay: 0.2
  # SmartRecruiters (careers.smartrecruiters.com/PHONEPELIMITED) — migrated
  # off Greenhouse (old job-boards.greenhouse.io/phonepe now 404s). "q"
  # keyword param is a loose server-side pre-filter only (full-text match,
  # not just title) — title/skill filters do the real work. country=in is a
  # reliable server-side filter. DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

atlassian_search:
  max_listings: 300
  inter_page_delay: 0.1
  # First-party JSON proxy (www.atlassian.com/endpoint/careers/listings)
  # fronting an iCIMS backend — keyword ignored server-side (byte-identical
  # response regardless of query), full ~234-job global pool returned in one
  # call, description already inline. DO NOT add require_tech_in_description
  # — titles carry real tech signal, not IT-services banding.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

intuit_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (same platform as Optum), org 27595. Keyword param genuinely
  # honored server-side; "l=" location param is ignored (rely on
  # is_india_job()). Pagination param is "p=" not "pg=" (differs from Optum's
  # tenant). Description needs a separate detail-page fetch.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

swiggy_search:
  max_listings: 200
  inter_page_delay: 0.1
  # MyNextHire (swiggy.mynexthire.com, embedded via careers-integration.js
  # iframe). No keyword/pagination params — full ~79-job pool cached once.
  # DO NOT add require_tech_in_description unless explicitly asked.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

zerodha_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Custom Vue 3 SPA, same-origin /api/jobs, zero params. Board has been
  # confirmed empty for 6+ months (verified via 3 Wayback Machine captures
  # spanning Feb-Aug 2026) — 0 matches expected, this is a genuine current
  # fact, not a fetcher defect. DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

amdocs_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Eightfold (amdocs.eightfold.ai) -- hit this host directly to bypass the
  # Incapsula bot-protection layer on the jobs.amdocs.com/careers.amdocs.com
  # branded fronts (same backend, confirmed via x-ef-* headers). Keyword
  # ignored server-side (identical pool for any query) -- location IS a real
  # server-side filter, sortBy=date IS genuinely newest-first. DO NOT add
  # require_tech_in_description -- titles carry real tech signal directly
  # (e.g. "Software Engineer(C# Developer)", "(Gen AI Developer)").
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

bnpparibas_search:
  max_listings: 700
  inter_page_delay: 0.2
  # Custom in-house Symfony site at group.bnpparibas (India Solutions Pvt
  # Ltd entity), Akamai-fronted -- plain requests hard-blocked at the edge
  # (not a JS challenge), Playwright/Firefox required throughout. City
  # facets (Mumbai/Bengaluru) are mandatory -- an unscoped keyword search
  # leaks global (Vienna/Milan/Manila) results. BNPP's own list-card HTML
  # mislabels ~257 genuine Mumbai postings as "Tamil Nadu" -- fetcher
  # overrides location with the known city facet rather than trusting BNPP's
  # own text, to avoid a false exclude_locations match. DO NOT add
  # require_tech_in_description -- titles carry explicit tech signal
  # (".NET Developer", "AI engineer", "Java Full Stack Developer").
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

citiustech_search:
  max_listings: 200
  inter_page_delay: 0.2
  # RippleHire (citiustech.ripplehire.com, token bCKlfz3OO8vQIgiM2vuI) --
  # same platform as LTIMindtree. "search" param genuinely filters
  # server-side. No location facet -- "geo" param silently zeroes results,
  # omit entirely; falls back to facility-name token matching. Titles are
  # level-banded ("Lead Engineer - I/II", "Technical Specialist - II") --
  # require_tech_in_description active.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

crisil_search:
  max_listings: 700
  inter_page_delay: 0.2
  # Zwayam ATS, shared multi-tenant endpoint (public.zwayam.com, companyId
  # 15438 -- base64 "MTU0Mzg=" for search, decoded "15438" for detail).
  # Keyword genuinely narrows server-side here (unlike Persistent) but
  # ignored anyway for simplicity -- full ~650-job pool (~594 India) cached
  # once. DO NOT add require_tech_in_description -- titles carry real tech
  # signal ("Senior AI Engineer - GenAI & ML solutions", "Dotnet Full stack
  # developer").
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

flipkart_search:
  max_listings: 8000
  inter_page_delay: 0.1
  # TurboHire ATS (flipkart.turbohire.co, backend thapi.azurewebsites.net).
  # Anonymous GET /api/token/noauth -> bearer JWT -> full ~6690-job
  # company-wide pool in one bulk POST, cached in-module (in-memory slice
  # after the one fetch -- inter_page_delay has no real network cost here).
  # max_listings MUST exceed the pool size or real matches deep in the list
  # get truncated. DO NOT add require_tech_in_description -- titles carry
  # real signal (SDE 1/2/3, "Senior Staff AI Engineer").
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

google_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Plain unauthenticated requests against Google's own careers SPA --
  # server-side-rendered AF_initDataCallback JSON blob in the raw HTML
  # (same pattern as Google Search/Flights), no auth/CAPTCHA/bot-challenge.
  # Keyword AND location genuinely filter server-side; full JD already
  # inline, no detail call needed. DO NOT add require_tech_in_description --
  # titles are specific ("Staff Software Engineer, Agentic AI Engineering"),
  # not generic level-banded.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

jioplatforms_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Legacy ASP.NET WebForms (careers.jio.com) -- static, non-session-bound
  # encrypted category tokens (could rotate on a Jio redeploy; re-scrape
  # frmJobCategories.aspx if fetch_jobs() ever returns 0). Only the
  # "Engineering & Technology" and "IT & Systems" categories are scraped --
  # the other 22 categories (tens of thousands of Freelancer/Ops/Sales jobs)
  # can never match title_family. Keyword/location form is a red herring --
  # it only filters which category TILES show, never actual jobs; ignored.
  # DO NOT add require_tech_in_description -- descriptions here are generic
  # boilerplate with zero concrete tech-stack naming, so Layer 4 would be moot.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

mastek_search:
  max_listings: 200
  inter_page_delay: 0.2
  # SAP SuccessFactors J2W classic theme (careers.mastek.com, company
  # masteklimi, career44.sapsf.com) -- server-rendered on first load, no JS
  # needed. Keyword search is UNSAFE: any non-matching query silently
  # returns a default fallback set instead of empty -- ignore entirely,
  # cache the full ~39-job India pool once via blank q= + locationsearch=india.
  # Pagination wraps around past the true total (drops the location filter
  # and returns unrelated global jobs) -- fetcher tracks total from the
  # first page and never requests past it. Titles are generic IT-services
  # bands hiding wildly different stacks -- require_tech_in_description active.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"

meta_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Internal GraphQL endpoint (metacareers.com/graphql) -- unauthenticated,
  # no CSRF-tied session, no CAPTCHA; office-filtered to India cities
  # (Bangalore/Gurgaon/Hyderabad/Mumbai/New Delhi), cached once. MAINTENANCE
  # RISK: the query's doc_id is an internal per-build identifier Meta can
  # rotate on a frontend deploy -- fails cleanly (404 -> RateLimitError)
  # rather than silently wrong; if fetch_jobs() starts erroring, re-open
  # /jobs in DevTools, filter Network for graphql, read the new doc_id off
  # CareersJobSearchResultsV2DataQuery.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

nykaa_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Skima AI ATS (careers.nykaa.com, server-rendered Astro SSR, new vendor
  # for this repo) -- whole board cached once, each job's detail page
  # fetched separately for description. Confirmed 0 live postings as of
  # 2026-08-31 (verified via empty sitemap.xml too, not a bot-block) --
  # self-heals once Nykaa posts through this portal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

policybazaar_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Trakstar Hire (legacy RecruiterBox rebrand, policybazaar.hire.trakstar.com
  # -- new ATS vendor for this repo). RSS feed (/jobfeeds/Policybazaar)
  # returns the whole board with inline descriptions in one request;
  # keyword/location ignored server-side. Only 1 open req system-wide as of
  # 2026-08-31 (PolicyBazaar's real engineering hiring appears to bypass
  # this board entirely, LinkedIn-only) -- near-zero matches expected
  # long-term, not a fetcher defect. RSS pubDate is a fixed per-tenant
  # template default, never a real date -- posting_date is "".
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

sharechat_search:
  max_listings: 200
  inter_page_delay: 0.2
  # MyNextHire (sharechat.mynexthire.com) -- same ATS as Swiggy. No
  # keyword/pagination params; full JD inline. Currently 0 engineering
  # postings (all Content/Ops roles) as of 2026-08-31 -- real current fact,
  # not a bug. DO NOT add require_tech_in_description unless explicitly asked.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

zomato_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Darwinbox ATS (eternal.darwinbox.in -- Zomato's parent renamed itself
  # Eternal Ltd; new vendor for this repo). Unauthenticated candidate API
  # (/ms/candidateapi/job), keyword/search ignored server-side. Board is
  # genuinely empty right now (jobscount: 0, verified twice) -- parsing
  # logic validated against two populated sibling Darwinbox tenants
  # (Zepto, Hetero) since the product's API is identical company-wide.
  # New postings will flow through automatically once Eternal/Zomato hires
  # through this portal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

dtcc_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Oracle HCM CE (ebxr.fa.us2.oraclecloud.com, site CX_1) -- same pattern as
  # Chubb/Amex/Hexaware/eClerx/BNY. No India location facet -- fetches
  # globally per keyword, filters India client-side via PrimaryLocation.
  # DO NOT add require_tech_in_description -- real engineering titles seen
  # directly (Software Engineer, SDET) in Chennai/Hyderabad.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

arcesium_search:
  max_listings: 50
  inter_page_delay: 0.2
  # Greenhouse board token "arcesiumllc" (~35 postings). Keyword/location
  # ignored server-side, full board cached once, description inline.
  # Current India titles are Infra/SRE/Principal-Engineer level (JVM/Python/
  # Scala/Kubernetes stack, no .NET or LangChain/RAG signal) and mostly
  # don't match the shared title_family phrasing either -- a real current
  # near-zero, same class as CitiusTech's leveling gap (see Wave 2 "Known
  # Gaps"). DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

aig_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant aig.wd1.myworkdayjobs.com, site "aig". Global req board
  # has ZERO India-tagged entries as of 2026-08-31 (confirmed via full
  # locationCountry/locations facet enumeration) despite AIG having a real
  # ~4,000-person India GCC -- a genuine current fact, not a fetcher defect.
  # Watch for "IN-" location prefixes meaning Indiana (US state), not India.
  # DO NOT add require_tech_in_description -- titles carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

bloomberg_search:
  max_listings: 200
  inter_page_delay: 0.15
  # Avature ATS (bloomberg.avature.net/careers) -- same platform as
  # Macquarie. Keyword genuinely filters server-side. Bloomberg is a
  # C++/Python/Java shop, not .NET (all .NET-style keywords return 0). All
  # current India engineering postings are in Pune, which is globally
  # excluded -- 0 matches expected, working as intended, not a bug.
  # DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

ice_search:
  max_listings: 50
  inter_page_delay: 0.2
  # iCIMS/Jibe (careers.ice.com/api/jobs, same family as spglobal_careers/
  # gallagher) -- offset ignored server-side, keyword narrows real results
  # but breaks pagination the same way; full ~36-job India pool cached once
  # instead. Description inline. DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

moodys_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (same vendor as Optum, org 49841, careers.moodys.com) --
  # different theme skin from Optum's (title <a> nested inside <h2>,
  # pagination param "p=" not "pg="). Keyword genuinely filters server-side;
  # location "l=" is inert, rely on is_india_job() instead.
  # DO NOT add require_tech_in_description -- descriptions
  # are genuinely role-specific, no boilerplate-reuse false-positive
  # pattern observed.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

nasdaq_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant nasdaq.wd1.myworkdayjobs.com, site Global_External_Site.
  # Facet key "Location_Country" (capitalized, like Marsh McLennan/State
  # Street); India WID c4f78be1a8f14da0ab49ce1162348a5e. limit hard-capped
  # at 20; pagination wraps around past the true total (MUFG-style bug) --
  # fetcher memoizes the first job ID per keyword to detect and stop it.
  # DO NOT add require_tech_in_description -- titles carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

swissre_search:
  max_listings: 200
  inter_page_delay: 0.2
  # SAP SuccessFactors J2W classic (careers.swissre.com, same family as
  # Nomura/Capgemini/SAP Labs/Mastek). Small ~33-job India pool, keyword
  # genuinely narrows server-side but ignored anyway (cache once, cheaper).
  # require_tech_in_description active: titles are mostly actuarial/ops
  # bands plus a handful of ambiguous engineering titles (SAP S4HANA,
  # ServiceNow, UiPath) with zero live .NET/C# mentions -- precision
  # tightener, same rationale as Sabre/Autodesk/Adobe/eBay.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

alphasense_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Greenhouse board token "alphasense" (~235 postings). Keyword/location
  # ignored server-side, full board cached once. India offices split across
  # "Remote - India" and bare city names (Bengaluru/Delhi/Pune) -- ", India"
  # appended only for recognized cities. DO NOT add
  # require_tech_in_description -- titles are specific, not generic bands.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

anz_search:
  max_listings: 200
  inter_page_delay: 0.2
  # SAP SuccessFactors J2W classic (careers.anz.com, same family as Nomura/
  # Capgemini/SAP Labs/Mastek/Swiss Re). Small ~20-job India pool (all
  # Bengaluru), keyword unreliable/loose -- cached once instead. Pagination
  # silently drops the India filter past the true total (new wraparound
  # variant) -- cache-once-and-slice sidesteps it. DO NOT add
  # require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

lloyds_search:
  max_listings: 60
  inter_page_delay: 0.2
  # Workday tenant lbg.wd3.myworkdayjobs.com, site "Lloyds_Technology_Centre"
  # -- the dedicated India GCC portal (Hyderabad-only by construction, no
  # country facet needed). limit hard-capped at 20; pagination wraps around
  # past the true total (MUFG-style bug) -- fetcher memoizes first job ID
  # per keyword. require_tech_in_description active: zero live postings
  # mention a real .NET/AI hard term, but 4 postings mention "SQL Server"
  # only as one entry in a generic ETL tools list (Pentaho/Snowflake/
  # BigQuery) -- same false-positive shape as Citi/State Street/Adobe.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  require_tech_in_description:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "LangChain"
    - "LangGraph"
    - "RAGAS"
    - "Langfuse"
    - "LangSmith"
    - "CrewAI"
    - "vector database"
    - "ChromaDB"
    - "Pinecone"
    - "generative ai"
    - "large language model"
    - "retrieval augmented generation"

uipath_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Ashby ATS (jobs.ashbyhq.com/uipath) -- new vendor for this repo. GET
  # api.ashbyhq.com/posting-api/job-board/uipath returns the full board (119
  # jobs) in one call, no pagination, keyword/location ignored server-side.
  # Full plain-text description inline. DO NOT add
  # require_tech_in_description -- titles/descriptions carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

visa_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday tenant visa.wd5.myworkdayjobs.com, site "Visa". locationCountry
  # facet works despite being nested under locationMainGroup (same quirk as
  # Adobe/WTW/Fiserv) -- genuine server-side India filter. India titles name
  # real tech signal directly ("Java/Python, RAG, GenAI Development",
  # "Python, Gen AI, Agentic AI") -- DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

blackrock_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (org 45831) -- a third distinct theme skin from Optum's and
  # Moody's (pagination "p="; page size 10; nested location markup). "l="
  # location param ignored server-side, rely on is_india_job(). Stack is
  # heavily Java/Python (Aladdin platform), not .NET -- the AI/ML track is
  # where BlackRock India matches come from. DO NOT add
  # require_tech_in_description -- titles carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

natwest_search:
  max_listings: 100
  inter_page_delay: 5.0
  # Talemetry ATS (jobs.natwestgroup.com) -- new vendor for this repo.
  # Cloudflare managed-challenge blocks plain requests entirely; needs
  # headless Chromium (Firefox gets stuck on the challenge indefinitely --
  # opposite of Honeywell/IBM/Tech Mahindra), with an enforced ~5s gap
  # between navigations or every request after the first gets re-challenged.
  # Full ~65-job India pool cached once, keyword/location ignored
  # server-side (loose/unreliable q= match). DO NOT add
  # require_tech_in_description -- titles here carry the tech signal
  # ("Python Software Engineer, VP") while descriptions are often generic
  # corporate boilerplate; Layer 4 would risk dropping genuine matches.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

zeta_search:
  max_listings: 30
  inter_page_delay: 0.2
  # Lever board token "zeta" (~20 postings, cache-once, same family as
  # CRED/Meesho/Paytm). country=="IN" used for India detection.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

juspay_search:
  max_listings: 20
  inter_page_delay: 0.2
  # Custom Next.js career-site JSON API (juspay.in/careers), cache-once, full
  # description inline. Small board (~10 jobs).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

m2p_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Custom Next.js career-site API (m2pfintech.com/careers), cache-once,
  # full description inline.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

yubi_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Zoho Recruit ATS (go-yubi.com/careers) -- new vendor for this repo.
  # Cache-once, full description inline.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

signzy_search:
  max_listings: 50
  inter_page_delay: 0.2
  # Keka ATS embedded-jobs widget (signzy.com/careers) -- new vendor for
  # this repo. Cache-once, full description inline.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

automationanywhere_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday. Keyword genuinely filters server-side. Description AI/ML/Python
  # heavy -- clean match, no .NET signal expected. DO NOT add
  # require_tech_in_description -- titles carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

servicenow_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Custom Umbraco CMS career site (careers.servicenow.com). Keyword ignored
  # server-side; India scoping applied server-side via ?country=India
  # (genuine server-side location filter). ~40-job India pool, cache-once.
  # DO NOT add require_tech_in_description -- specific engineering/research
  # titles, not generic bands.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

datarobot_search:
  max_listings: 50
  inter_page_delay: 0.2
  # WordPress AJAX career-page endpoint (datarobot.com/careers). Small board
  # (16 total worldwide as of 2026-08-31) -- confirmed 0 India postings via
  # the site's own location-filter list (no India/Bangalore/Hyderabad option
  # exists at all), a genuine current fact not a fetcher defect.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

thomsonreuters_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday. Keyword genuinely filters server-side. DO NOT add
  # require_tech_in_description -- titles carry real signal.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

glean_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Greenhouse (glean.com/careers). Cache-once, ignores keyword/location
  # server-side, same family as Groww/Razorpay/AlphaSense. DO NOT add
  # require_tech_in_description -- specific product-engineering titles.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

perfios_search:
  max_listings: 20
  inter_page_delay: 0.2
  # Darwinbox "candidatev2" SPA behind Cloudflare (perfios.darwinbox.in).
  # Tiny board (~3 postings), cache-once, ignores keyword/location
  # server-side, same family as Zeta/Juspay/M2P. DO NOT add
  # require_tech_in_description -- board is too small to need it.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

unitedairlines_search:
  max_listings: 60
  inter_page_delay: 0.2
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  # DO NOT add require_tech_in_description -- titles are specific enough
  # (e.g. "Senior AI Engineer - Cybersecurity", "Engineer - Generative AI")
  # that the shared Layer 3 skill check already produces zero false
  # positives at this pool size (32 jobs total).

nvidia_search:
  max_listings: 250
  inter_page_delay: 0.2
  # Workday (nvidia.wd5.myworkdayjobs.com/NVIDIAExternalCareerSite). India
  # filtered server-side via the nested "locationHierarchy1" facet
  # (tenant-specific WID, not the shared cross-tenant India GUID); ~236
  # India jobs. Keyword search genuinely narrows server-side. DO NOT add
  # require_tech_in_description -- titles are specific enough (e.g.
  # "Senior AI/ML Software Engineer", "System Software Engineer - Deep
  # Learning") that the shared Layer 3 skill check is sufficient.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

shell_search:
  max_listings: 200
  inter_page_delay: 0.2
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations
  # DO NOT add require_tech_in_description -- Shell's own India titles are
  # specific ("Software Engineer ENDUR") and the pool is far too small
  # (33 total) for Layer 4's precision rationale to apply.

pfizer_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday (pfizer.wd1.myworkdayjobs.com/PfizerCareers); keyword genuinely
  # narrows server-side (unlike most tenants in this repo). Small, volatile
  # India pool (~17-22 jobs) skewed toward manufacturing/QA/medical roles.
  # DO NOT add require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

boeing_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (jobs.boeing.com), same vendor as Optum. Tenant ignores the
  # free-text "?l=" location param (opposite of Optum's tenant) -- India
  # scoping uses the dedicated location-facet URL instead. India pool is
  # tiny right now (2 jobs) and churns fast.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

pepsico_search:
  max_listings: 200
  inter_page_delay: 0.2
  # iCIMS/Jibe (pepsicojobs.com), not "custom". offset param is ignored and
  # limit hard-caps at 100 -- fetcher caches per-keyword like
  # UBS/Deutsche Bank. DO NOT add require_tech_in_description -- not
  # applicable (see PLAYBOOK.md Key Bugs).
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

walmart_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Workday (walmart.wd504.myworkdayjobs.com/WalmartExternal) -- single
  # global tenant, no separate India Tech sub-board. 300 (not the usual
  # 200) because the unfiltered India pool is ~277 jobs and the single
  # broadest keyword alone totals 233. DO NOT add
  # require_tech_in_description -- Layer 3 alone already produces a
  # healthy non-noisy match ratio here.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

disney_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (disneycareers.com, org 391) -- same vendor as
  # Optum/Boeing, NOT "custom". keyword/location query params are ignored
  # server-side even on the India location-facet URL; fetcher caches the
  # whole India pool (paginated, ~27 jobs currently) once per process.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

expedia_search:
  max_listings: 200
  inter_page_delay: 0.2
  # WordPress "Appcast Job Search" plugin (careers.expediagroup.com), not
  # "custom" -- verified via DevTools from scratch, not the prior secondary
  # research label. Fully server-rendered HTML, keyword and filter[country]
  # both genuinely server-side. India pool is tiny (13 jobs, Bangalore +
  # Gurgaon only). DO NOT add require_tech_in_description -- every live
  # posting sampled is pure Python/AI-stack with zero .NET/C# mentions;
  # Layer 3 alone is already precise here.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

geaerospace_search:
  max_listings: 100
  inter_page_delay: 0.2
  # Workday (geaerospace.wd5.myworkdayjobs.com/GE_ExternalSite), NOT "custom"
  # -- the public careers.geaerospace.com frontend is actually Phenom People
  # (same family as Morningstar/United Airlines), but every job's own
  # applyUrl reveals the real backend ATS is Workday -- same "ATS skin over
  # a different real ATS" shape as Boeing/TalentBrew. No locationCountry
  # facet on this tenant -- India scoped via 3 hardcoded city WIDs (2
  # Bengaluru + Pune), same pattern as Barclays' city WIDs. Current entire
  # India pool is tiny (9 jobs) and zero are software-engineering titled
  # right now -- a genuine current fact, not a fetcher defect. DO NOT add
  # require_tech_in_description -- pool far too small for Layer 4.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

gehealthcare_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Phenom People (careers.gehealthcare.com) -- NOT Oracle HCM CE, the
  # prior recon guess from URL shape alone was wrong (see PLAYBOOK.md Key
  # Bugs / Wave 5). No location facet actually narrows server-side --
  # fetches globally per keyword (genuinely server-side), filters India
  # client-side via each job's own `country` field. Real apply/description
  # backend is Workday (gehc.wd5.myworkdayjobs.com) even though the public
  # search frontend is Phenom People. DO NOT add
  # require_tech_in_description -- titles are genuine, not generic
  # IT-services banding; Layer 3 alone is the right precision level here.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

cisco_search:
  max_listings: 300
  inter_page_delay: 0.2
  # Phenom People (careers.cisco.com/global/en/india/search-results) -- NOT
  # "custom". ~275 total India jobs, real full-board India search (the
  # assigned etr-jobs URL is a tiny 13-job curated subset, not the full
  # board). Bare "AI" is a no-op keyword here (returns the entire pool, same
  # as no filter) -- default_keywords' "AI engineer"/"generative ai
  # engineer" passes are slow but harmless (dedup handles it). DO NOT add
  # require_tech_in_description -- pool is small/precise enough that Layer 3
  # alone is sufficient.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

nutanix_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Jobvite (jobs.jobvite.com/nutanix) -- careers.nutanix.com itself is
  # Cloudflare-managed-challenge-gated, but the actual board lives on this
  # separate, un-gated Jobvite domain; no Playwright needed. Keyword genuinely
  # narrows server-side (loose OR/token matching, not exact phrase). No
  # usable generic "India" location facet (l=India returns "No results
  # found"; only an exact "Bangalore, India" composite value works) --
  # fetches globally per keyword, India filtered client-side same as
  # Genpact/Fiserv/FactSet. DO NOT add require_tech_in_description -- small
  # pool (~35 India jobs), current India titles are infra/kernel/storage
  # with no hard .NET/C# or AI/ML signal as of 2026-09-03.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

schneiderelectric_search:
  max_listings: 200
  inter_page_delay: 0.2
  # iCIMS via Jibe Careers Site Builder (careers.se.com/api/jobs), NOT
  # "custom" -- same platform family as PepsiCo/Gallagher/S&P Global
  # Careers. 533 total India postings; keyword/location genuinely filter
  # server-side; pagination does not wrap around (unlike PepsiCo's tenant on
  # the same ATS family). Confirmed genuine zero for both tracks as of
  # 2026-09-03 -- India engineering stack here is Java/Spring/Angular/Azure/
  # embedded-IoT, not .NET or LangChain/RAG. DO NOT add
  # require_tech_in_description -- wouldn't change today's already-zero
  # outcome.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

qualcomm_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Eightfold (careers.qualcomm.com / qualcomm.eightfold.ai), NOT "custom" --
  # same platform family as Microsoft/Morgan Stanley/Amdocs/HSBC. Unlike
  # HSBC, the public pcsx/search endpoint is fully enabled here. `q` keyword
  # param is genuinely ignored server-side; `location=India` genuinely
  # filters server-side (~565 India jobs of ~1961 global). DO NOT add
  # require_tech_in_description -- India titles are dominated by
  # embedded/hardware roles (correctly excluded), no generic-title precision
  # problem like the IT-services shops this filter was built for.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

abinbev_search:
  max_listings: 200
  inter_page_delay: 0.15
  # Sense CRM domain (ab-inbev-gcc.sensehq.com) fronting a Skillate
  # career-page backend ("hosting_type":"SKILLATE") -- a genuinely new ATS
  # pattern for this repo, only half-matching the prior "SenseHQ" guess.
  # pageSize silently no-ops above 10; pagination breaks past page 2
  # regardless of true total -- fetcher unions oldest-10 + newest-10 sort
  # orders to cover the whole (currently tiny, ~15-job) India board. DO NOT
  # add require_tech_in_description -- board is analytics/ops/finance-heavy,
  # not a generic-title precision problem.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

novartis_search:
  max_listings: 200
  inter_page_delay: 0.2
  # Workday (novartis.wd3.myworkdayjobs.com/Novartis_Careers) -- tenant
  # guess held up but the exact site code (underscored, not hyphenated)
  # needed independent discovery. ~130 India jobs (Hyderabad); keyword is a
  # genuine full-text match against JD body (SAP Labs pattern), not ignored.
  # Pagination wraps around past the true total (page-1-first-ID memo). 0
  # current matches -- every India title is level-banded
  # (Director/Manager/Analyst), not a fetcher defect. DO NOT add
  # require_tech_in_description.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

delta_search:
  max_listings: 200
  inter_page_delay: 0.15
  # Avature (dth.avature.net) -- Delta's own "Delta Technology Hub" India
  # (Bangalore-only) microsite, found via that branded name, NOT the
  # Talent500 lead from initial recon (Talent500 has a real, working,
  # company-scoped API but Delta's own profile there returns 0 jobs right
  # now) and NOT Eightfold (delta.eightfold.ai has PCSX disabled, same as
  # HSBC, with no usable anchor job). Keyword/location ignored server-side,
  # cache-once pattern; clean `?jobOffset=N` pagination, no wraparound.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

lufthansa_search:
  max_listings: 400
  inter_page_delay: 0.2
  # milch & zucker "Global Jobboard" API (api-apply.lufthansagroup.careers)
  # -- same product family as Deutsche Bank's Beesite API, different
  # tenant. No country-level search filter exists -- whole global pool
  # (~355 jobs) cached once, filtered client-side via
  # PositionLocation.CountryCode == "IN". Real India presence is Lufthansa
  # Systems India Private Ltd. (Bangalore) plus occasional Lufthansa Cargo
  # AG (Mumbai) -- not Pune. DO NOT add require_tech_in_description --
  # small pool, not needed.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

astrazeneca_search:
  max_listings: 200
  inter_page_delay: 0.2
  # TalentBrew (careers.astrazeneca.com, org 7684) -- same vendor/HTML
  # pattern as Optum/Boeing/Disney. Keyword and the generic
  # `/search-jobs?l=India` location filter are both ignored server-side --
  # fetcher caches the dedicated India-facet page once per process. 34
  # total India jobs (Bengaluru/Chennai/Mumbai only -- no Hyderabad facet on
  # this tenant despite that being AstraZeneca's usual India framing). DO
  # NOT add require_tech_in_description -- titles are the limiting factor
  # here, not an overly-broad skill list.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

dazn_search:
  max_listings: 60
  inter_page_delay: 0.1
  # Pinpoint ATS (dazn.pinpointhq.com/postings.json) -- new ATS vendor for
  # this repo. Keyword/location ignored server-side, full ~117-job global
  # board cached once and filtered to India client-side (word-boundary
  # \bindia\b guard against the known "US - Indiana" false positive). DO
  # NOT add require_tech_in_description -- board is small, titles
  # descriptive enough.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

salesforce_search:
  max_listings: 200
  inter_page_delay: 0.1
  # ATS backend is Workday (salesforce.wd12.myworkdayjobs.com), but the
  # public careers site never calls the Workday CXS search API -- it
  # fetches a static, unauthenticated CDN JSON export instead
  # (a.sfdcstatic.com/digital/xsf/careers/prod/jobs_2.json, ~1479 global
  # jobs incl. full HTML descriptions inline; a near-identical jobs_1.json
  # also exists but has an empty Job_Description for 1459/1479 entries --
  # use jobs_2.json only). Keyword/location ignored server-side (no search
  # API to filter with); ~121 India jobs (Hyderabad/Bengaluru/Gurgaon/
  # Mumbai) filtered client-side via each job's own Countries list, cached
  # once per process. DO NOT add require_tech_in_description -- titles are
  # descriptive, not generic IT-services banding.
  keywords: *default_keywords
  locations: *india_locations
  exclude_locations: *default_exclude_locations

notifications:
  email:
    recipient: ""     # e.g. "you@example.com"

  telegram:
    bot_token: ""     # e.g. "123456:ABC-DEF..."
    chat_id: ""       # e.g. "987654321"
