# Job Watcher Configuration
# Do NOT commit this file to git — it may contain notification secrets.

search:
  keywords:
    - "software engineer"
    - "full stack engineer"
    - "backend engineer"
  locations:
    - "India"

matching:
  # Keep a job only if its description mentions at least one of these skills.
  skills:
    - ".NET"
    - "C#"
    - "ASP.NET"
    - "SQL Server"
    - "Angular"
    - "TypeScript"
    - "Azure"
    - "full stack"
    - "backend"

  # Keep a job only if its title contains at least one of these level strings.
  # Roman numerals (II, III) and Arabic numerals (2, 3) are normalised before
  # matching, so "Software Engineer II" also catches "Software Engineer 2".
  level_keywords:
    - "Software Engineer II"   # also matches "Software Engineer 2" via normalisation
    - "SWE II"                 # also matches "SWE 2"
    - "SDE II"                 # also matches "SDE 2"
    - "SDE2"                   # no-space Microsoft variant
    - "Senior Software Engineer"
    - "Sr Software Engineer"
    - "SSE"

  # Reject a job immediately if its title contains any of these terms.
  exclude_terms:
    - "intern"
    - "internship"
    - "new grad"
    - "principal"
    - "director"
    - "data scientist"

notifications:
  email:
    recipient: ""     # e.g. "you@example.com"

  telegram:
    bot_token: ""     # e.g. "123456:ABC-DEF..."
    chat_id: ""       # e.g. "987654321"
