name: Heartbeat

on:
  # Disabled for local-primary mode. Manual dispatch remains available for
  # emergency verification without starting an automatic watcher chain.
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: echo "heartbeat"
